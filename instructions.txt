git clone https://github.com/Siddhesh-Bhande/NewWebDev.git

to add file from remote:

git pull -v


to add file from local to remote:

git pull -v 

git status

git add .

git commit -m "commit message"

git push -v

git status

demo change

someone else changed
